import Ember from 'ember';

// export function isOutsidePresenter() {
//   // var format = params[0];
//   // var date = params[1];
//   // return moment(date).format(format);
// }

export default Ember.Helper.helper(presenter);
