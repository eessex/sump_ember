<header>
  <h1 id="logo">{{link-to 'the Sump' 'index'}}</h1>
  <nav id="main">
    {{link-to 'Events' 'events'}}
    {{link-to 'About' 'about'}}

    {{#if session.isAuthenticated}}
    <a href="#" {{action 'invalidateSession'}}>Logout</a>
    {{/if}}
  </nav>
</header>
<main>

  {{outlet}}
</main>
