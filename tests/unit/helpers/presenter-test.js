import { presenter } from 'sump-ember/helpers/presenter';
import { module, test } from 'qunit';

module('Unit | Helper | presenter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = presenter([42]);
  assert.ok(result);
});
